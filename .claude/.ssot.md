# ShELF — Design SSOT

> Source: standalone prototype `Downloads/ShELF Dashboard (standalone).html` (1.5 MB, bundler-wrapped).
> Real content: an HTML template + 9 inline JS modules (React 19 dev, ReactDOM, data seeds in `window.SHELF_DATA`, `window.STRAT`, statement helpers, components, tweaks panel, strategie panel) plus DM Mono/DM Sans woff2.
> This is the canonical design contract. When code disagrees with this file, this file wins (engineering should ask the user before changing the contract).

---

## 1. Surfaces (high-level)

Top-level chrome is a left **Pillar** sidebar + a `.main` column with a `.topbar` (greeting + search + `.nav`) and a scrolling `.canvas`. Tabs in `.nav`:

- **Shelf** — `PromptCard` on top, then `.grid-bar` (folder count + "New folder"), then the bookmark `.grid` of `FolderCard`s plus a `GoalCard` (drag/sort via `useSortable` + FLIP).
- **Visual Flow** — node-graph view (nodes/edges/sectors/grazeland); prototype shows an empty-state placeholder, real impl lives in `src/components/VisualFlowPanel.tsx`.
- **Strategie** — finance dashboard: KPI strip → 5-year projection chart → ladder (order-of-operations) → "Spending by week" → cashflow ring + Allocation bar + Pots + Life pillars. Modal: `StatementEditor` (the editable monthly income + dated spending book).
- **Hopper** — single buy-line: a "chute" with a queue of `puck`s falling toward a `tray` ("Next to buy"). Toss in, bump, skip, bought.
- **LLM Console overlay** (extension): top-bar + Prompt Library sidebar injected over a navigated tab — never an iframe.
- **Pillar** (sidebar, always visible): shelf greeting + `.todo-add` + `.todo-list` with focus pins + prompt count + completion log.

Global UI shells (only in the proto): `.bg-diffuse` ambient radial-gradient glow behind everything, animated by `@keyframes drift` (34s), and a floating `TweaksPanel` (theme/identity/accent/currency/surface/density/radius). `⌘K` focuses the search.

---

## 2. Design tokens

All tokens live on `:root` in the inline `<style>`. Themes are `[data-theme="day"]` and `[data-theme="sap"]` (the default is dark, no attribute). Surface variant `[data-surface="glass"]`; density `[data-density="compact|comfy"]` (regular = unset).

### Dark (default `:root`)

```css
:root {
  --accent: #16b981; --accent-bright: #34d399;
  --accent-deep: #0c8f66; --accent-ink: #d4f3e7;

  --bg: #0b0b0d; --bg-grad-a: #0d0d10; --bg-grad-b: #0a0a0c;
  --panel: #141417; --surface: #161619;
  --surface-2: #1b1b20; --surface-3: #212128; --inset: #0e0e11;

  --line: rgba(255,255,255,0.065);
  --line-strong: rgba(255,255,255,0.11);
  --line-faint: rgba(255,255,255,0.04);

  --fg: #f3f3f4; --fg-2: #c9c9cf;
  --muted: #91919b; --dim: #66666f; --faint: #4b4b53;

  --hue-zinc: #8b8b95; --hue-orange: #e08648; --hue-green: #34c891;
  --hue-blue: #6595ee; --hue-purple: #a384df; --hue-rose: #e07a93;

  --sh-1: 0 1px 2px rgba(0,0,0,0.45);
  --sh-2: 0 2px 6px rgba(0,0,0,0.35), 0 1px 2px rgba(0,0,0,0.4);
  --sh-3: 0 10px 30px -10px rgba(0,0,0,0.65), 0 2px 6px rgba(0,0,0,0.4);
  --sh-pop: 0 24px 60px -18px rgba(0,0,0,0.8), 0 4px 12px rgba(0,0,0,0.5);

  --r-card: 16px; --r-inner: 11px; --r-pill: 999px;
  --pad: 20px; --gap: 16px;

  --font: "DM Sans", system-ui, sans-serif;
  --mono: "DM Mono", ui-monospace, monospace;
}
```

### Day theme (`[data-theme="day"]`)

Accent shifts to warm amber: `--accent: #d97706`, `--accent-bright: #f59e0b`, `--accent-deep: #b45309`, `--accent-ink: #7c4a0c`. Background warm bone: `--bg: #f4f3f1`, grads `#fafaf9 → #e7e5e4`, `--panel: #fbfaf9`, `--surface: #ffffff`, `--surface-2: #f5f4f2`, `--surface-3: #ebe9e6`, `--inset: #f1efec`. Lines `rgba(28,25,23,0.09 / 0.16 / 0.055)`. Text `--fg #1c1917`, `--fg-2 #292524`, `--muted #57534e`, `--dim #78716c`, `--faint #a8a29e`. Hues: zinc #78716c, orange #ea7c2f, green #1f9d6b, blue #3b78d8, purple #8857c9, rose #d6557a. Shadows tinted `rgba(28,25,23,*)`.

### SAP theme (`[data-theme="sap"]`)

Accent shifts to SAP blue: `--accent: #0070f2`, bright `#1b90ff`, deep `#0058c4`, ink `#084298`. Background cool slate: `--bg #dee6f0`, grads `#e8eef5 → #d4dce8`, `--panel #eef3f9`, `--surface #fff`, `--surface-2 #eef2f8`, `--surface-3 #e2e9f3`, `--inset #eaf0f7`. Lines tinted blue: `rgba(0,112,242,0.14 / 0.26)` + `rgba(10,30,60,0.06)`. Text `#0a0a0a / #1a2433 / #4a5568 / #687891 / #9aa7bd`. Hues: zinc #687891, orange #e06c00, green #107e3e, blue #0070f2 (=accent), purple #7858c8, rose #d1456b. Shadows tinted `rgba(0,60,130,*)`.

### Per-theme overrides

- On `day`/`sap`: `.toss-btn`, `.mini-btn--buy`, `.stack-badge`, `.cbox--on svg`, `.se-btn--primary`, `.btn-buy` switch text to `#fff` (light surface needs white-on-accent).
- On `day`/`sap`: many `--accent-bright` usages flip to `--accent-deep` (better contrast on light surfaces). Search the CSS for `[data-theme="day"], [data-theme="sap"]` selectors — they all follow this pattern.
- `.prompt-body` flips to `#5a7065` on day, `#46586f` on sap.
- `.se-date` calendar picker filter: dark uses `invert(0.7)`, light uses `none`.

### Glass surface (`[data-surface="glass"]`)

Replaces solid surfaces with translucent whites: `--surface: rgba(255,255,255,0.045)`, `--surface-2: 0.07`, `--surface-3: 0.1`, `--panel: 0.025`, `--inset: 0.03`, `--line: rgba(255,255,255,0.09)`.

### Density

`[data-density="compact"]` → `--pad:15px; --gap:11px;`
`[data-density="comfy"]` → `--pad:24px; --gap:20px;`
regular (unset) = root default `20px / 16px`.

### Tweaks-driven CSS vars

JS writes `--accent`, `--accent-bright`, `--accent-deep`, `--accent-ink`, `--r-card`, `--r-inner` (= `max(6, radius-5)`), and the `data-surface`, `data-density`, `data-theme` attributes onto `<html>`. Theme switching toggles `.no-transition` for one frame to avoid mid-interpolation freezes.

---

## 3. Typography

- Body: `DM Sans` (system-ui fallback). Mono: `DM Mono` (ui-monospace fallback). Both shipped as woff2 (latin + latin-ext, weights 400 and 500).
- Greeting: 28px / 600 / `letter-spacing -0.025em`. Smile word in `--accent-bright`.
- Card title (`.card-title`): 15.5px / 600 / `-0.01em` / `--fg`.
- Card eyebrow (`.card-eyebrow`): 10px / 600 / `letter-spacing 0.15em` / uppercase / `--dim`.
- Statement editor title (`.se-title`): 23px / 600 / `-0.025em`.
- Statement eyebrow (`.se-eyebrow`): 11px / 600 / `0.16em` / uppercase / `--accent-bright` (becomes `--accent-deep` on light themes).
- Hopper title: 30px / 600 / `-0.025em`. Ladder title (`.ld-title`): 24px / 600 / `-0.025em`.
- KPI / tally values: `--mono`, 17px, 500, `font-variant-numeric: tabular-nums`, `--fg`. Mini money displays use `--mono` at 11–13px.
- Body text default 13–14px / `--muted` for context, `--fg`/`--fg-2` for primary. Mini-text 10.5–12px for hints.
- All monospace numeric runs (any `b`/`.…-amt`/`.kpi`/`.se-…-sum`/`.flow-row b`) get `font-variant-numeric: tabular-nums` and `--mono`.

---

## 4. Layout primitives

### Shell
`.bg-diffuse` (fixed, inset -15%, blurred radial gradients of `--accent`, `--hue-blue`, `--hue-purple`, animated by `drift`). `.shell` is a flex row containing `Pillar` + `.main`. `.main` stacks a sticky `.topbar` and a scrolling `.canvas` (padding `26px 32px 60px`). `.canvas-inner` holds the active tab.

### `.card`
Background `var(--surface)`, border `1px solid var(--line)`, radius `var(--r-card)`, shadow `var(--sh-2)`, padding `var(--pad)`. `.card-head` is flex row (flex-start, gap 14, margin-bottom 16). `.card-eyebrow` and `.card-title` as above.

### `.seg` segmented control
`display: inline-flex; padding: 3px; gap: 2px; background: var(--inset); border: 1px solid var(--line); border-radius: 10px;`. `.seg-btn` is unstyled until hover (gains `--fg-2`) and `.on` (gains `--surface-2` background + `--fg` + `var(--sh-1)`).

### `.ghost-btn`
Surface button, `1px solid var(--line)`, `--muted` text, gap 6, padding/radius matching mini controls; hover lifts to `--surface-2` + `--line-strong` + `--fg`. SVG icons sized 14×14. `.ghost-btn.ok` variant: accent-tinted border + `--accent-bright` text.

### `.mini-ghost`
12px / 600 / `--fg-2` on `--surface-2`, border `--line`, radius 8, padding 6×10. SVG 13×13. Hover → `--surface-3` + `--line-strong`.

### `.nav-btn`
Top-bar tabs. Default: `--surface`, border `--line`, `--muted`. `.nav-btn--on` swaps to accent-tinted surface + `--accent-bright` text. On light themes the "on" state uses `--accent-deep`.

### 12-col grid
`.strat-grid { display: grid; grid-template-columns: repeat(12, 1fr); gap: 14px; align-items: start; }` with `.span-8` and `.span-4`. Used inside the Strategie surface to lay out KPI row, projection (8) + sidebar (4), etc.

### Modal / backdrop pattern
Two backdrop families share a recipe:
- `.se-backdrop` / `.ld-backdrop`: `position: fixed; inset: 0; z-index: 200+; display: grid; place-items: center; padding: 28px; background: color-mix(in srgb, var(--bg-grad-b) 55%, rgba(0,0,0,0.6)); backdrop-filter: blur(8px) saturate(120%); animation: se-fade 0.16s ease;`
- `.se-modal` / `.ld-modal`: `background: var(--panel); border: 1px solid var(--line-strong); border-radius: calc(var(--r-card) + 6px); box-shadow: var(--sh-pop); animation: se-rise 0.22s cubic-bezier(0.2,0.8,0.3,1);` — width capped (`min(900px, 100%)` for statement, `min(560px, 100%)` for ladder), `max-height: calc(100vh - 56px)`, internal flex column with scroll.
- Outside-click on backdrop closes. `Escape` closes; if a sub-popover is open (e.g. month picker), first Escape closes the popover, second Escape closes the modal.

### `.toast`
Small `--accent`-tinted pill that briefly appears top-right with a check icon.

---

## 5. Per-surface anatomy

### 5.1 Shelf (BookmarkGrid)

Wrapper: `.canvas-inner > [React.Fragment]` containing:
- `<PromptCard data={D} onToast={…} />` — top promo / library callout.
- `.grid-bar` with `.grid-hint` ("N folders · drag to rearrange") and `.grid-actions` containing a `ghost-btn` "New folder".
- `.grid` — flex/grid container, drag-sorted via `useSortable` (FLIP animation). Children: many `<FolderCard>` + one `<GoalCard>`.

**FolderCard** (`.folder`): per-folder `--hue` driven by `folder.hue` (one of `var(--hue-*)`). Children:
- `.folder-head` (cursor grab; grip dots `.folder-grip span`; title `.folder-title`; count `.folder-count`; pseudo `::before` for the hue dot).
- `.folder-body` containing `.bm` (small bookmark row) and `.bm-big` (large card) entries; hover dims siblings to 0.45 and full-opacity on target. `{ sep: true }` items render a separator.
- Big bookmark shape (`{ big: true, …, initials, color }`) renders a Monogram tile.

**GoalCard** (`.goal`): card with `.goal-eyebrow`, `.goal-title`, progress `.goal-bar > .goal-fill` (hue mix `--hue-blue`), `.goal-foot` with `.goal-pct` and `.goal-cta` ghost button.

**Search results** (`.results`): replaces tab content when `query` is non-empty. `.result-head` count line, then anchor rows `.result` containing a `Favicon`, `.result-meta` (`.result-title` + `.result-url`), and `.result-folder` tag. Empty → `.empty-state`.

Drag/click/keyboard: drag to reorder folders; click any `.bm` opens its URL; `⌘K` (or `Ctrl-K`) focuses search.

Reads/writes: each folder `{ id, title, hue, dot, bookmarks: [{title,host,url} | {sep:true} | {big,title,host,url,initials,color}] }`.

### 5.2 Strategie

Wrapper: `<StrategiePanel currency onCurrency onToast />`. Layout uses `.strat-grid` for major sections.

Anatomy (top-to-bottom):
- `.strat-eyebrow` headline + currency switcher.
- **KPI strip** — row of `.kpi` cards: invested · saved this month · savings rate · runway. Each card: `.kpi-label` (eyebrow), `.kpi-value` (mono 22–24px), `.kpi-sub` (delta with `.up`/`.down` color).
- **Projection chart** (`.proj-card` containing `.proj-svg`): area under `.proj-line` and a contribution band. `.proj-dot` marks the final balance. Header has a returns segmented control (Conservative/Balanced/Growth) and a monthly-contribution slider.
- **Ladder** (`.rung`s): vertical list of order-of-operations rungs. Each `.rung` carries `--step-hue`; states `.rung--done`, `.rung--active`, `.rung--queued` style `.rung-mark` and `.rung-tag` accordingly. Click a rung → opens `LadderModal` (`.ld-backdrop` + `.ld-modal` with `.ld-hero` washed in the rung hue, `.ld-status`, `.ld-step` eyebrow, `.ld-title`, and `.ld-body` listing accounts + history).
- **Spending by week** card (`.wk-chart`): rows `.wk-row` of `86px 1fr 60px` columns — `.wk-meta` (`.wk-name`, `.wk-range`), `.wk-track` (bar with stacked `.wk-seg`s colored by `STMT_CATS` hues, dashed `.wk-avgtick`), `.wk-amt` (over-budget rows gain `.over` → `--hue-orange`). Foot has `.wk-avg-key` legend dash + colored `.wk-leg-dot`s per category.
- **Cashflow ring** (`.ring`): inline SVG donut `circle.ring-track` + `circle.ring-fill` (stroke-dasharray driven). `.ring-mid > b` percent, `.ring-cap > span/small` label.
- **Allocation bar**: stacked segments colored by `allocation.slices[].hue`, `.alloc-row` per slice with label, pct, and amount.
- **Pots** (`.pot-card` per `STRAT.pots[]`): name, target/saved progress bar, monthly contribution, `.pot-flag` if `fromHopper`.
- **Life pillars** (`.life-card` per `STRAT.pillars[]`): label, metric, note, state pill in `--accent-bright` (`tone: "good"`) or `--hue-orange` (`"warn"`).
- **Cashflow rows** (`.flow-rows > .flow-row`): label · `b.pos`/`b.neg` mono value · `.total` final summary row.

Modal: `StatementEditor` — see Section 6.

### 5.3 Visual Flow

Prototype only ships a placeholder `.empty-state` ("A node-graph view of your bookmarks lives here in the full app."). Real implementation: `src/components/VisualFlowPanel.tsx` — `@xyflow/react` graph of todos as nodes/edges, with context menus, collision avoidance, sector handles, grazeland plane, hopper integration. Persists via `VISUAL_FLOW_KEY` in `useShelfStorage`.

### 5.4 Hopper

Wrapper: `.hopper` (max-width 600, centered). Children:
- `.hopper-head` — `.hopper-title` "Hopper", `.hopper-sub` ("Toss things in the top. The one that sinks to the bottom is next to buy."), `.hopper-count` with `<b>N</b><span>in line</span>`.
- `.chute` — the column. Contains:
  - `.mouth` — input `.fld` + `.mouth-btn` ("+ Toss in"). Enter key tosses.
  - `.queue` of `.puck` rows when there are waiting items. Each `.puck` has `.puck-pos` (position number), `.puck-name`, `.puck-acts` (bump down `I.arrowDown` + discard `I.x`).
  - `.funnel` (visual taper SVG between queue and tray) — only rendered when `waiting.length > 0`.
  - `.tray` — `.tray-label` ("Next to buy") + `.tray-row` with `.tray-name` and `.tray-acts` (`.btn-ghost` "Not yet" → skip-to-top, `.btn-buy` "Bought" → remove + toast).
- Empty state: `.hopper-empty` "The hopper's empty…".

Interactions: `toss` appends, `bump` moves to bottom, `skip` moves to top, `bought` removes + emits toast.

Shape: `{ id, name, next? }` per item.

### 5.5 Pillar (sidebar)

Wrapper: `<aside class="pillar">` with `.pillar-head` (shelf name or current greeting) and `.pillar-body` (column, gap 20).

Children:
- Top pins (top-6) — small grid of pinned bookmarks (`pins[]` from data).
- `.todo-add` — composer (input + add).
- `.todo-list` — `.todo` items with `.todo-main` (`.todo-row > .todo-title + .todo-link + .todo-x`), and either a tag row (`.todo-tag tag--{tagClass}` + optional `.todo-sub`) or a bare `.todo-sub`. `.todo--focus` brightens with accent tint. `.todo--done` halves opacity and line-throughs the title.
- Prompt count + completion log (closed/done items list).

Data shape: `{ id, title, sub?, tag?, tagClass?, link?, done?, focus?, focusRank? }`. Focus pins are ranked.

### 5.6 LLM Console + Prompt Library

Prompt Library (`.prompt-card`) appears at the top of the Shelf surface and inside the LLM overlay sidebar.
- `.prompt-top` row: left has `.prompt-eyebrow` ("Prompt Library") + `.prompt-title`; right has `.prompt-tools` containing `.prompt-rows-sel` (toggle pills 1/2/3 lines), `.prompt-count`, `.prompt-add` ("+ Prompt").
- `.prompt-grid` is a 2-column grid (`1fr 1fr`) of `.prompt` cards. Hover lifts (`translateY(-1px)`) + accent-tinted border. Click copies `p.full` via `navigator.clipboard` and toasts "Copied …".
- `.prompt-h` (name + "Click to copy" hint). `.prompt-body` is monospace, clamped to N lines via `-webkit-line-clamp`. Tokens like `[SYSTEM]` get `.tok-sys` styling.

LLM Console: in extension, the content script `public/shelf-llm-overlay.js` injects a top-bar + sidebar overlay on top of an existing tab (never an iframe). Background service worker `public/background.js` handles `openLLMConsole`, `closeLLMConsole`, `openPromptLibrary` messages.

Prompt shape: `{ id, name, version, lines: [{ sys?, rest }], full }`.

---

## 6. Statement editor (zoomed in)

The "Import statement" experience — opened from Strategie. Mounts `<StatementEditor open book order startMonth currency onClose onSave />`.

### Outer JSX shape

```
.se-backdrop  (mousedown on self → close)
  .se-modal
    .se-head
      .se-head-l
        .se-eyebrow  <SI.file/> "Statement"
        h2.se-title  "What's coming in & going out"
        p.se-lede    (one-liner: feeds cashflow, savings rate, projection)
      button.se-close <SI.x/>
    .se-periodbar
      .se-stepper
        button.se-nav  (prev month)
        .se-monthpick
          button.se-month.se-month--btn(.open)
            span.se-month-name {Month YYYY} <SI.chev .se-month-caret/>
            span.se-month-net(.neg)  "+/−$N /mo"
          .se-monthmenu  (listbox of months + "Start <next>" appender)
        button.se-nav  (next; becomes <SI.plus/> at last month)
      .se-gran
        .se-gran-lab "Spending"
        .seg
          button.seg-btn(.on) "Whole month"
          button.seg-btn(.on) "By week"
    .se-body  (grid: 1fr 1fr, gap 22; collapses to 1col below 760px)
      section.se-col.se-col--in
        .se-col-head
          span.se-col-title  <.se-pill.se-pill--in><SI.in/></.>  Income  <small.se-col-hint>monthly</small>
          span.se-col-sum  {fmt(incomeBase)}
        .se-rows
          .se-row × n
            input.se-label
            .se-amt > span.se-cur + input.se-amt-input
            button.se-del <SI.trash/>
        button.se-add  "+ Add income"
      section.se-col.se-col--out
        .se-col-head
          span.se-col-title  <.se-pill.se-pill--out><SI.out/></.>  Spending  <small.se-col-hint>(active week | "dated")</small>
          span.se-col-sum  {fmt(expShownSum)}
        .se-weekrow
          button.se-wkflip (prev)
          .se-weekstrip
            button.se-wk(.on) × weeks
              .se-wk-bar > .se-wk-fill   (height = totals[i]/maxWeek * 100%)
              .se-wk-amt
              .se-wk-lab "W1".."W5"
          button.se-wkflip (next)
        .se-rows
          .se-row × n
            span.se-catdot   (background = STMT_CATS[cat].hue)
            input.se-label
            input.se-date type="date"   (min/max clamped to month bounds)
            select.se-cat                 (CAT_KEYS options)
            .se-amt > span.se-cur + input.se-amt-input
            button.se-del
        button.se-add "+ Add expense [in WK]"
    .se-foot
      .se-tallies
        // whole-month view:
        .se-tally  "Income" b{fmt(income)}
        span.se-op "−"
        .se-tally  "Spending" b{fmt(expense)}
        span.se-op "="
        .se-tally.se-tally--net(.neg)  "Left to save"  b{fmt(surplus)}  <small>N% saved · per month</small>
        // week view:
        .se-tally  "{Wk} spent"  b{fmt(weekSpend)}
        ·  .se-tally "Weekly avg" b{fmt(avgWeek)}  ·  .se-tally Δ vs avg
      .se-actions
        button.se-btn.se-btn--ghost "Cancel"
        button.se-btn.se-btn--primary "Save"
```

### Semantics

- **USD-base storage.** Every amount in `book` is integer USD. `cur = window.CURRENCIES[currency]`; `shown(base) = round(base * cur.rate)`, `toBase(shown) = round(shown / cur.rate)`. Display uses `Intl.NumberFormat(cur.locale, { style:'currency', currency:cur.code, maximumFractionDigits:0 })`. Currency prefix symbol (`.se-cur`): `Kč | € | $`.
- **Income is monthly carry-forward.** Income rows `{ id, label, amt, kind }` are not dated. Advancing months via the next-arrow (or "Start <month>") calls `cloneMonth(prevMonth, nextKey)`: income copied verbatim; expenses get a new `date` preserving day-of-month, clamped to the new month's length.
- **Expenses are dated.** Shape `{ id, label, cat, amt, date }` where `date = "YYYY-MM-DD"`. `window.weekOfDate(monthKey, date)` bins each into a week. Rows are sorted chronologically.
- **Week strip math.** `window.monthWeeks(key)` produces Monday-aligned weeks clamped to the month — `[{ idx, startDay, endDay, label: "W{idx}", range: "MMM d–e" }]`. The first "week" begins on day 1 regardless of weekday, ending at the next Sunday (or month-end). Per-week totals from `weekOfDate==idx` sums. Strip bar height = `round(total / maxWeek * 100)%`. Clicking a bar toggles to that week in "By week" mode; clicking the active bar returns to month mode. Selected week gets `.se-wk.on`.
- **Whole month / By week toggle** (`.seg` inside `.se-gran`): `gran` is `"month" | "week"`. In `"week"` mode, the spending list filters and the column hint shows `{activeWeek.label} · {activeWeek.range}`; in `"month"` mode the hint is `dated`. New-expense button reads "Add expense in W{n}" while in week mode.
- **Income/spending column heads.** `.se-pill.se-pill--in` is a 24×24 7px-radius square tinted with `color-mix(--accent 20%, transparent)` containing an `in` arrow; `.se-pill--out` is `--hue-rose` tinted with an `out` arrow. `.se-col-sum` is mono, 15px / 500, tabular-nums.
- **Row anatomy** (`.se-row`): catdot (8×8 circle colored by category hue, expenses only) · `.se-label` text input · `.se-date` type=date input (expenses only) · `.se-cat` select (expenses only) · `.se-amt` group · `.se-del` trash button. Focus styling: 3px ring of `color-mix(--accent 16%, transparent)` and border `color-mix(--accent 55%, transparent)`.
- **`.se-amt` + `.se-cur` pattern.** `.se-amt` is the bordered container (`background var(--inset)`, 9px radius, width 116). Inside, `.se-cur` is the prefix symbol (mono, `--dim`) and `.se-amt-input` is the actual text input (mono, `tabular-nums`, right-aligned, no border, transparent bg). The input is `type="text"` + `inputMode="numeric"`. **Live raw-string buffer**: `value` reads `r.amt ? shown(r.amt).toLocaleString(cur.locale) : ""` (empty when 0). `onChange` strips non-digits with `replace(/[^\d]/g, '')`, parses int, calls `toBase(n)`. No decimal point, no fractional cents — integers in display units converted back to USD-base.
- **Foot tally** (whole month): `Income − Spending = Left to save`, with savings rate `(surplus/income)*100` rounded; net pill `.se-tally--net` (`.neg` if negative). Week mode tally instead shows `{Wk} spent · Weekly avg · Δ vs avg`.
- **Save / Cancel.** `commit()` strips empty rows (`r.label.trim() || r.amt`), then `onSave(cleaned, keys, viewKey)`. Cancel = `onClose`. Mousedown on backdrop self also closes.
- **Escape behavior.** Double-duty: if `pickerOpen` (month listbox), first Escape closes the picker only; otherwise Escape closes the editor.
- **Month picker** (`.se-monthmenu`): listbox of `.se-mo-opt`s. Active month gets `.on` + a `.se-mo-check` with `SI.check`. Each option: `.se-mo-name` (Month YYYY) + `.se-mo-net` (signed mono `+/−$N`, `.neg` if surplus<0). Last item `.se-mo-add` (+ icon + "Start {next-month-abbr}") creates the next month via `cloneMonth(lastMonth, newKey)`.

Category palette (from `STMT_CATS` in module `04_…`):

| key       | label        | hue                  |
|-----------|--------------|----------------------|
| housing   | Housing      | `var(--hue-blue)`    |
| food      | Food         | `var(--hue-green)`   |
| transport | Transport    | `var(--hue-orange)`  |
| home      | Home & bills | `var(--hue-purple)`  |
| fun       | Fun          | `var(--hue-rose)`    |
| health    | Health       | `var(--accent)`      |
| shopping  | Shopping     | `var(--hue-zinc)`    |
| other     | Other        | `var(--faint)`       |

---

## 7. Data model

### Persisted keys (the real extension)

These live in `chrome.storage.local`, accessed exclusively via `src/hooks/useShelfStorage.ts`. Naming follows `shelf-*`. Known keys (extension code, not the prototype): `shelf-strategie`, `shelf-buylist`, `shelf-visual-flow`, `shelf-pillar-todos`, `shelf-bookmarks-layout`, `shelf-bookmarks-colors`, `shelf-bookmarks-size`, `shelf-bookmarks-separators`, `shelf-bookmarks-goal`, `shelf-bookmarks-hidden`, `shelf-prompts`, `shelf-theme`, `shelf-grazeland`, `shelf-bin`, `shelf-llm-console-url`, `LOW_PERFORMANCE_MODE_KEY`. Any new persisted setting **must** be added there (key, default, hydration, setter) plus a type in `src/types/grid.ts`.

### Prototype seeds — `window.SHELF_DATA`

```js
{
  shelfName: "Hey Jimmy, smile",
  pins: [{ id, title, url, host }],          // top-6 pinned
  todos: [{ id, title, sub?, tag?, tagClass?, link?, done?, focus?, focusRank? }],
  prompts: [{ id, name, version, lines: [{ sys?, rest }], full }],
  folders: [{ id, title, hue: "var(--hue-…)", dot: bool,
              bookmarks: [{title,host,url} | {sep:true} | {big:true,title,host,url,initials,color}] }],
  goal: { eyebrow, title, pct, cta },
  hopper: [{ id, name, next? }],
}
```

### Prototype seed — `window.STRAT`

```js
{
  cashflow: { income, expenses, toInvest, toPots, importedTx },
  statements: { current: "YYYY-MM", order: ["2026-01",…], byMonth: {
    "2026-01": { income: [{id,label,amt,kind}], expenses: [{id,label,cat,amt,date}] },
    …
  }},
  positions: { invested, emergencySaved, emergencyTarget },
  assumptions: { horizonYears, returns: [{id,label,rate}], defaultReturn, inflation },
  ladder: [{ id, title, note, status: "done"|"active"|"queued", pct?, target?,
             icon, hue, blurb, accounts: [{name,tag,balance}],
             history: [{date,label,amt}] }],
  allocation: { profile, slices: [{id,label,pct,hue}] },
  pots: [{ id, name, target, saved, monthly, fromHopper }],
  pillars: [{ id, label, metric, note, state, tone: "good"|"warn" }],
}
```

Default seed runs `2026-01` through `2026-04` with synthetic $4,200 salary + $150–$400 side income, fixed rent/health/subscriptions/utilities, and variable groceries/dining/transport/shopping/other. Expenses are programmatically expanded from `STMT_SPEC` via `_buildVariable` (deterministic FNV hash + mulberry32) so weekly distribution is stable per `month|label`.

### Currency table — `window.CURRENCIES`

```js
{
  USD: { code: "USD", locale: "en-US", rate: 1     },
  EUR: { code: "EUR", locale: "de-DE", rate: 0.92  },
  CZK: { code: "CZK", locale: "cs-CZ", rate: 23.2  },
}
```

`window.fmtMoney(usd, cur, opts)` converts and formats (`{ abbr: true }` produces `$12.3k`, `€450`, `25k Kč` style abbreviations).

### Projection math

```js
window.project = (principal, monthly, annualReturn, months) => {
  const r = annualReturn / 12;
  const pts = [];
  let bal = principal, contrib = principal;
  for (let m = 0; m <= months; m++) {
    if (m > 0) { bal = bal * (1 + r) + monthly; contrib += monthly; }
    pts.push({ m, bal, contrib, growth: Math.max(0, bal - contrib) });
  }
  return pts;
};
```

Compounding is **end-of-month**: the prior balance grows by `r/12` then receives the contribution. `pts[0]` is the starting state (`bal = principal`, `contrib = principal`, `growth = 0`). `pts[months]` is the final state; the chart plots `bal` (top) and `contrib` (band) with the gap representing growth.

### Date helpers (all on `window`)

- `daysInMonth("YYYY-MM")` → number (uses `new Date(y, m, 0).getDate()`).
- `dayStr("YYYY-MM", day)` → `"YYYY-MM-DD"`.
- `monthWeeks("YYYY-MM")` → `[{ idx, startDay, endDay, label: "W{i}", range: "MMM d–e" }]`, Monday-aligned, clamped.
- `weekOfDate("YYYY-MM", "YYYY-MM-DD")` → 1-indexed week within the month.

---

## 8. Themes

- The `App` component writes `data-theme` on `<html>` (omitted for dark) plus `data-surface` and `data-density`, and sets `--accent`/`--accent-bright`/`--accent-deep`/`--accent-ink`/`--r-card`/`--r-inner` directly from the Tweaks state.
- `THEME_ACCENT` map drives the per-theme default accent (emerald / amber / SAP blue). Picking a theme via Tweaks resets the accent to its theme default.
- During a theme swap, `.no-transition` is added to `<html>` for one rAF tick to suppress mid-interpolation freezes on var-driven colors.
- Palette diffs: dark = inky-black + emerald; day = warm-bone + amber, lighter shadow tints; sap = cool-slate + cobalt, blue-tinted lines (`rgba(0,112,242,*)`).
- Light themes (day + sap) systematically swap `--accent-bright` for `--accent-deep` on read-heavy spots (badges, eyebrows, KPI ups, projection tags) for contrast. Accent-filled buttons (`.se-btn--primary`, `.btn-buy`, `.toss-btn`, `.stack-badge`, `.mini-btn--buy`, `.cbox--on svg`) keep `#fff` text on light themes.

Tweaks panel (`TweaksPanel` from module `06_…jsx`): floating panel exposing Theme (radio dark/day/sap), Identity (greeting text), Accent (color radio), Currency (USD/EUR/CZK), Surfaces (solid/glass), Density (compact/regular/comfy), Corner radius (slider 6–22 px).

---

## 9. Iconography

All inline SVG, 24×24 viewBox (some 26×14 for funnel), `stroke="currentColor"` with `strokeWidth ~1.8–2.4`, `strokeLinecap="round"`, `strokeLinejoin="round"`. Two icon namespaces:

- `I.*` (general — module `03_…`): `search`, `check`, `link`, `plus`, `chev`, `arrowDown`, `x`, `funnel`, `target`.
- `SI.*` (strategie — module `04_…`): `check`, `lock`, `plus`, `upload`, `chev`, `arrow`, `hopper`, `x`, `trash`, `in`, `out`, `file`, `shield`, `gift`, `flame`, `vault`, `leaf`, `growth`, `target`, `clock`.

Semantic mapping: shield/vault/leaf/flame/gift/growth icons are used on Strategie ladder rungs (one per `ladder[i].icon`). `file` is the Statement eyebrow icon. `in`/`out` arrows fill the income/spending column pills. `funnel` is the chute taper between Hopper queue and tray. `target` is the "Next to buy" tray label.

A `<Monogram>` component (module `02_…`) renders square initials tiles for "big" bookmarks; `hueFromString(domain)` returns a deterministic hue. `<Favicon host=…>` wraps `window.favicon(host)` = `https://www.google.com/s2/favicons?domain={host}&sz=64`.

---

## 10. Open questions / known gaps

- **Visual Flow** is a stub in the prototype — only an `.empty-state`. The real design contract for sectors/grazeland/bin/handles lives in `src/components/VisualFlowPanel.tsx`; this SSOT cannot mirror it. If the prototype is meant to be the source of truth for Visual Flow visuals, that surface needs to be filled in.
- **LLM Console overlay** is described conceptually but not stylized in the prototype (it lives entirely in the extension content script). No tokens for the overlay top-bar or sidebar are present here; the prototype's PromptCard styling is the closest reference.
- The Tweaks `radius` slider drives only `--r-card` and `--r-inner` — modal corner radii are `calc(var(--r-card) + 6px)`. Pots/pillar cards and other inner panels do not all consume `--r-inner`; spot-check before relying on the slider for every surface.
- Statement editor: the brief mentions "no inline date input in current version", but the prototype HAS an `input.se-date` per expense row. Treat the prototype as canonical (date input present); confirm with the user if production should drop it.
- Currency rates are hard-coded constants (`USD 1`, `EUR 0.92`, `CZK 23.2`) — no FX-fetch loop. Live rates would be a contract change.
- The bundled prototype loads React 19 **development** builds — performance characteristics and console output won't match production.
- `goal` data (`pct: 98`, "Coursera — Agentic RAG") is hard-coded; there's no shape for multiple goals or goal progress sources.
- Glass surface variant is exposed in Tweaks but several cards (`.kpi`, ladder rungs, pot cards) read direct hex `--surface` and may not fully translate to `rgba()` glass; verify visually.
- No accessibility audit: most interactive `<span>`/`<div>` patterns lack roles; `.se-modal` carries `role="dialog" aria-modal` but most ghost buttons do not declare `aria-label`. Production should fix this.

---

## 11. Session log

### 2026-06-03 — Memberships in StatementEditor
- **Context:** User shipped an updated standalone prototype (`ShELF Dashboard (standalone) (2).html`); review surfaced Memberships as the only net-new Strategie feature. Visual Flow unchanged in this drop.
- **Changes:**
  - `src/types/grid.ts` — new `MembershipRow` interface; `memberships: MembershipRow[]` added to `StrategieState` with seeded defaults (Netflix/Spotify/YT Premium/ChatGPT/iCloud+/Notion). Backward-compat parsing in `normalizeStrategie`.
  - `src/components/Strategie/icons.tsx` — added `IcoRepeat`, `IcoPencil`.
  - `src/components/Strategie/StatementEditor.tsx` — props gain `memberships` + 4th `onSave` arg `memberships`. New Memberships block under Income column: tile grid (hover tooltip, hover edit affordance), tap-to-pause (greyscale + strikethrough overlay), inline add/edit panel with name + plan + USD-base price + 10-swatch color palette + 2-char mono badge. Members commit only on the Save button.
  - `src/components/Strategie/StrategiePanel.tsx` — threads `state.memberships` into the editor; `onSaveStatement` signature widened.
  - `src/hooks/useShelfStorage.ts` — `strategieSaveStatement` accepts optional 4th `memberships` arg and persists into `shelf-strategie`.
  - `src/index.css` — appended `.se-mem*`, `.se-tile*`, `.se-sw`, `.se-pill--mem`, `@keyframes se-mem-in` block (verbatim from prototype).
- **Outcome:** worked — clean `tsc -b`. No UI verification yet in browser.
- **Follow-ups:** Spot-check tile/tooltip on day + sap themes; confirm prototype "no inline date input" vs current `.se-date` discrepancy is still tolerable; if user pauses a membership, decide whether `memTotal` should still ignore paused (current behavior matches prototype: paused are excluded from sum).

---

## 12. Decisions journal

### Worked
- **Persist memberships through `StrategieState` (not modal-local).** — Tiles survive reopen and theme swap; commits on Save.
  **Why:** Prototype scoped members to modal lifetime via `window.STRAT.memberships`; in production that loses every edit on close. Routing through `useShelfStorage` keeps the CLAUDE.md rule ("any persisted setting goes through `useShelfStorage`") intact and gives free import/export.
  **Applies to:** `StrategieState.memberships`, `strategieSaveStatement` in `useShelfStorage.ts`, `StatementEditor` props.
  **Date:** 2026-06-03.

- **Extend `onSave` signature with optional 4th arg `memberships`, instead of adding a parallel `onSaveMemberships` callback.** — Single commit path; backward compatible.
  **Why:** Two callbacks would force two separate stores/coalescing; one save event matches the prototype's "Save to dashboard" UX where membership edits are committed atomically with the statement. Making the 4th arg optional keeps existing 3-arg call sites type-clean without a shim.
  **Applies to:** `StatementEditorProps.onSave`, `StrategiePanelProps.onSaveStatement`, `strategieSaveStatement`.
  **Date:** 2026-06-03.

- **Encode the brand color via a `--brand` CSS custom property on the tile, swatch, and preview.** — All tile state (bg, border, hover glow, mono text, focused-swatch ring) derives from one variable.
  **Why:** Prototype already does this and it keeps every per-brand tint in CSS via `color-mix(in srgb, var(--brand) …%, …)` — adding a new color anywhere only sets `--brand`. Avoids inline JS-computed colors and stays theme-aware (tiles auto-restyle on glass/light surfaces because the mix targets `var(--surface-2)`).
  **Applies to:** `.se-tile`, `.se-tile--preview`, `.se-sw`.
  **Date:** 2026-06-03.

### Did NOT work
- _none recorded this session._
